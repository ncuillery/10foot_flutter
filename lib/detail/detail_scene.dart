import 'package:flutter/material.dart';

class DetailScene extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Detail scene", style: TextStyle(color: Colors.white)),
      ),
    );
  }
}
